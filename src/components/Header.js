import React from 'react';

const Header = () => {
    return (
        <header className="border-primary">
            <h1 className="m-0">Imaginary</h1>
        </header>
    );
};

export default Header;
